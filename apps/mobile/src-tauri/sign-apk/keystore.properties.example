# Android 签名密钥库配置
# 
# 使用说明：
# 1. 运行 setup-android-signing.sh 脚本会自动复制此文件到 gen/android/keystore.properties
#    或手动复制: cp sign-apk/keystore.properties.example gen/android/keystore.properties
# 2. 填入您的密钥库信息（替换下面的占位符）
# 3. 确保使用绝对路径
#
# 生成密钥库：
#   macOS/Linux: ./sign-apk/generate-keystore.sh
#   Windows: sign-apk\generate-keystore.bat
#
# 密钥库默认位置：
#   macOS/Linux: ~/upload-keystore.jks
#   Windows: %USERPROFILE%\upload-keystore.jks

storePassword=your-keystore-password
keyPassword=your-key-password
keyAlias=upload
storeFile=/Users/your-username/upload-keystore.jks

# Windows 路径示例（使用双反斜杠）:
# storeFile=C:\\Users\\your-username\\upload-keystore.jks
#
# Linux 路径示例:
# storeFile=/home/your-username/upload-keystore.jks
