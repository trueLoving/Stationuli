# Android 签名密钥库配置
# 
# 使用说明：
# 1. 复制此文件为 keystore.properties：
#    cp sign-apk/keystore.properties.example sign-apk/keystore.properties
#
# 2. 编辑 sign-apk/keystore.properties，填入您的密钥库信息（替换下面的占位符）
#
# 3. 运行签名配置脚本：
#    ./sign-apk/setup-android-signing.sh
#
#    脚本会自动：
#    - 将 sign-apk/keystore.properties 复制到 gen/android/keystore.properties
#    - 配置 build.gradle.kts 添加签名设置
#
# 4. 即使删除 gen 目录，重新构建后运行 setup-android-signing.sh 即可恢复签名配置
#
# 生成密钥库：
#   macOS/Linux: ./sign-apk/generate-keystore.sh
#   Windows: sign-apk\generate-keystore.bat
#
# 密钥库默认位置：
#   macOS/Linux: ~/upload-keystore.jks
#   Windows: %USERPROFILE%\upload-keystore.jks
#
# 重要提示：
#   - 确保使用绝对路径
#   - 此文件包含敏感信息，已添加到 .gitignore，不会提交到版本控制

storePassword=your-keystore-password
keyPassword=your-key-password
keyAlias=upload
storeFile=/Users/your-username/upload-keystore.jks

# Windows 路径示例（使用双反斜杠）:
# storeFile=C:\\Users\\your-username\\upload-keystore.jks
#
# Linux 路径示例:
# storeFile=/home/your-username/upload-keystore.jks
